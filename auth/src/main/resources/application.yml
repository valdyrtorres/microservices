server:
  port: 8083
  servlet:
    context-path: /auth
    
spring: 
  application:
    name: auth
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
      dialect: org.hibernate.dialect
  jmx:
    enabled: false
  datasource:
    url: jdbc:mysql://localhost:3316/auth?allowPublicKeyRetrieval=true&sslMode=DISABLED&useSSL=false&serverTimezone=UTC
    username: valdir
    password: valdir123
    
security:
  jwt:
    token:
      secret-key: chave_microservices
      expire-length: 360000
      